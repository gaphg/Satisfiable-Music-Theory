## Build
have dune installed (do thru opam) and use ocaml 5.1.1 or greater.

run `dune build` to build, `dune exec satzart`  to run the code in main.ml, and
`dune test` to run the code in test_satzart.ml